@model SV18T1021108.DomainModel.Shipper
@{

}
<div class="box box-primary">

    <form class="form-horizontal" action="~/shipper/delete/@Model.ShipperID" method="post">
        <div class="box-body">
            <div class="box-header with-border">
                <div class="form-group">
                    <label class="control-label col-sm-2">Họ:</label>
                    <div class="col-sm-10">
                        <p class="form-control-static">@Model.ShipperName </p>
                    </div>
                </div>
            </div>
            <div class="box-header with-border">
                <div class="form-group">
                    <label class="control-label col-sm-2">Họ:</label>
                    <div class="col-sm-10">
                        <p class="form-control-static">@Model.Phone </p>
                    </div>
                </div>
            </div>
            <div class="box-header with-border">
                <div class="form-group">
                    <label class="control-label col-sm-2">Họ:</label>
                    <div class="col-sm-10">
                        <p class="form-control-static">@Model.ShipperID </p>
                    </div>
                </div>
            </div>

        </div>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                @if (SV18T1021108.BusinessLayer.CommonDataService.InUsedShipper(Model.ShipperID))
                {
            <p class="text-danger"> Không được xóa người giao hàng</p> }
        else
        {
            <button type="submit" class="btn btn-danger">
                <i class="fa fa-remove"></i> Đồng ý xóa
            </button>}

                <a class="btn btn-warning" href="~/shipper">
                    <i class="fa fa-ban"></i> Quay lại
                </a>
            </div>
        </div>
    </form>
</div>